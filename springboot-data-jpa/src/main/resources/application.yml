server:
  port: 9090

spring:
  application:
    name: springboot-data-jap

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123456
    url: jdbc:mysql://localhost:3306/test?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8
  jpa:
    show-sql: true
    hibernate:
      # 自动更新表
      ddl-auto: update

